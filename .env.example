# Copy this file to .env and fill in real values. .env is gitignored.
#
# Generate a strong JWT_SECRET locally, e.g.:
#   openssl rand -hex 32
# Keep this stable across restarts — regenerating it invalidates all
# existing sessions/tokens and forces every user to log in again.
JWT_SECRET=
